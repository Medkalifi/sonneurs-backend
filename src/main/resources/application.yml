spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sonneursdb
    username: app-user
    password: appuser
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
